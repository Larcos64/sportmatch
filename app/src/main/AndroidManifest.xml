<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sportmatch"
        tools:targetApi="31">
        <activity
            android:name=".usecases.register.AthleteRegistrationActivity"
            android:exported="false"
            android:label="@string/title_activity_athlete_registration"
            android:theme="@style/Theme.Sportmatch" />
        <activity
            android:name=".usecases.register.EstablishmentRegistrationActivity"
            android:exported="false"
            android:label="@string/title_activity_establishment_regsitration"
            android:theme="@style/Theme.Sportmatch" />
        <activity
            android:name=".usecases.register.RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".usecases.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".usecases.auth.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>